# 공주비서 2.0 의존성
# 디스코드 게이트웨이
discord.py>=2.4
python-dotenv>=1.0

# LangGraph 에이전트 (도구 호출 루프 + 대화기억을 내장한 create_react_agent)
langgraph>=0.3
langgraph-checkpoint-sqlite>=2.0   # SqliteSaver: 채널별 대화기억 저장소

# Claude 모델 어댑터 (기존 anthropic 직접호출을 대체)
langchain-anthropic>=0.3

# OpenAI 호환 서버 어댑터 — 로컬 vLLM(Qwen)을 뇌로 바꿔 낄 때만 쓴다.
# .env에 VLLM_BASE_URL이 없으면 import조차 안 된다. (agent.py _build_model)
langchain-openai>=0.3

# 노션 REST 직접 호출 + 이미지 압축
# (노션 MCP 서버는 #9 Phase 1에서 걷어냈다 — langchain-mcp-adapters도 같이 빠짐)
httpx>=0.27
Pillow>=10.0

# 상태 서버(/health) — Phase 3 WireShark 캡처용. 봇과 같은 asyncio 루프에서 실행.
fastapi>=0.110
uvicorn>=0.30
